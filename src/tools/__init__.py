"""Turkí Price Intelligence — Tool Layer.

Provides clean, self-contained functions that an Orchestrator Agent
(or any external caller) can invoke as tools. Each function wraps
existing pipeline logic without duplicating it.
"""