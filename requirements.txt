# Turkí Price Intelligence - Dependencies
# ============================================
# Python 3.10+ required
# SQLite3 is built-in to Python stdlib (no pip install needed)

# HTTP / Scraping
# curl_cffi is the primary HTTP client — impersonates Chrome TLS fingerprint
# to bypass basic bot protections (Cloudflare, Akamai, PerimeterX).
# Falls back to Playwright (CloakBrowser) if curl_cffi fails, then LLM fallback.
curl_cffi>=0.7.0
requests>=2.31.0
httpx>=0.26.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
fake-useragent>=1.4.0

# Browser automation (CloakBrowser wraps Playwright with stealth patches)
cloakbrowser>=0.3.0
playwright>=1.40.0
playwright-stealth>=1.0.0

# Data models / config
pydantic>=2.0.0
PyYAML>=6.0

# CLI output
rich>=13.0.0

# File locking (cron safety)
filelock>=3.13.0

# Dashboard
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0diskcache
tenacity
